# 小说叙事验证工具

本目录是小说结构管理工具的验证仓库，用于沉淀原型、需求说明和后续实验代码。当前仓库只管理 `验证工具` 内部内容，不直接污染外层小说正文和支撑材料。

## 新会话入口

迁移、重建 Git 或新开 Codex 会话后，先读：

```text
CODEX_START_HERE.md
```

再读：

```text
CURRENT_HANDOFF.md
```

## 当前基准

当前基准原型：

`原型包/2026-06-22-叙事验证工具-基准时间轴原型-v1/`

当前主页面：

```text
原型包/2026-06-22-叙事验证工具-基准时间轴原型-v1/source/index.html
```

当前章节函数页面：

```text
原型包/2026-06-22-叙事验证工具-基准时间轴原型-v1/source/chapter-workbench.html
```

这一版作为后续继续迭代的基础。它已经较好地表达了以下方向：

- 以时间轴作为对齐底盘。
- 以章节作为基础单元。
- 用多轨事件块表达不同故事线、事件和人物参与条。
- 点击章节或事件只选中，不自动跳转。
- 章节选中后通过“进入章节函数”进入章节工作台。
- 支持基准时间轴投影数据集下拉切换。
- 支持标准时间轴和章节时间轴两类显示思路。

早期 v6、v7、v9 原型是设计过程记录，不再作为当前实现基准。

## 当前文件结构

```text
验证工具/
  CODEX_START_HERE.md
  CURRENT_HANDOFF.md
  README.md
  .gitignore
  小说叙事验证工具-策划案.md
  叙事验证工具-需求规格说明-v0.1.md
  设计说明/
  原型包/
    2026-06-22-叙事验证工具-基准时间轴原型-v1/
      source/index.html
      source/chapter-workbench.html
      source/data/
      source/scripts/
```

## 查看原型

先在 `source` 目录启动 HTTP 服务：

```powershell
cd C:\OpenCodeWorkSpace\TestProject\文章重写\验证工具\原型包\2026-06-22-叙事验证工具-基准时间轴原型-v1\source
npx --yes vite --host 127.0.0.1 --port 4173
```

再打开：

```text
http://127.0.0.1:4173/index.html
```

不建议直接用 `file://` 打开，因为页面需要读取 JSON 投影数据集。

## 迭代规则

- 新方向或重要评审修正，继续创建新的版本包，不覆盖当前基准。
- 临时测试、脚本和缓存必须留在本仓库内部。
- 外层小说正文、设定、章节文件不属于本仓库的默认改动范围。
- 页面读取的 JSON 统一称为“基准时间轴投影数据集”，不是最终结构化小说数据库。
