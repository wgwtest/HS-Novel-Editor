# P0.1 文档治理底座机测记录

## 基本信息

| 项 | 内容 |
| --- | --- |
| 时间 | 2026-06-29 00:16 |
| 节点 | `P0.1 文档治理底座` |
| 范围 | 文档治理骨架、根入口接入、忽略项边界、当前原型静态校验 |
| 结论 | 通过；等待人工确认后关闭 P0.1 |

## 执行命令

在当前基准原型 `source` 目录执行：

```powershell
node scripts/validate-story-datasets.mjs
node scripts/validate-character-participation.mjs
node scripts/validate-axis-modes.mjs
node scripts/validate-event-min-width.mjs
node scripts/validate-hit-priority.mjs
node scripts/validate-visual-encoding.mjs
```

在仓库根目录执行：

```powershell
rg --files DOC/CODEX_DOC
git status --short --ignored
git diff --check
rg -n "^验证作品/$|^验证作品/" .gitignore
```

## 结果摘要

- `validate-story-datasets.mjs`：通过。
- `validate-character-participation.mjs`：通过。
- `validate-axis-modes.mjs`：通过。
- `validate-event-min-width.mjs`：通过。
- `validate-hit-priority.mjs`：通过。
- `validate-visual-encoding.mjs`：通过。
- `rg --files DOC/CODEX_DOC`：能列出正式文档根下的入口、计划、合同、验收和交接文档。
- `git status --short --ignored`：显示根文档修改、新增 `DOC/`，并显示 `!! 验证作品/`。
- `.gitignore`：第 7 行包含 `验证作品/`。
- `git diff --check`：无 whitespace 错误；仅出现 Git 的 LF/CRLF 工作区提示。

## 待人工确认

用户确认以下内容后，P0.1 可标记为验收通过：

- `DOC/CODEX_DOC/` 作为正式工程文档根。
- 当前 WBS 和节点合同粒度可作为后续默认协作结构。
- `设计说明/` 和 `原型包/` 暂不迁移，只通过索引纳入治理。
