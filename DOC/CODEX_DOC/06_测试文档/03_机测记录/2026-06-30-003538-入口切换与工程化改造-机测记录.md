# 入口切换与工程化改造机测记录

## 1. 记录信息

| 项 | 内容 |
| --- | --- |
| 时间 | 2026-06-30 00:35:38 |
| 范围 | P2.4 app 工程化解耦、正式入口切换 |
| 运行目录 | `C:\CodexWorkSpace\CodexProject\HS-Novel-Editor\app` |
| 当前正式入口 | `app/index.html` |
| 当前浏览器 URL | `http://127.0.0.1:4174/index.html` |

## 2. TDD RED 记录

新增 `app/scripts/validate-formal-entrypoint.mjs` 并接入 `npm run check` 后，旧文档仍指向原型包，校验失败：

```text
Error: CODEX_START_HERE.md must name app/index.html as the current formal page.
```

## 3. 静态校验

执行：

```powershell
npm run check
```

通过项：

```text
App shell validation passed.
Formal entrypoint validation passed.
Module boundary validation passed.
Story dataset validation passed.
Character participation validation passed.
Axis mode validation passed.
Event min width validation passed.
Hit priority validation passed.
视觉编码规则校验通过。
View state management validation passed.
Wide canvas and two-axis drag validation passed.
```

## 4. 运行时校验

本地服务：

```powershell
cd C:\CodexWorkSpace\CodexProject\HS-Novel-Editor\app
npm run dev
```

访问：

```text
http://127.0.0.1:4174/index.html?p244=1
```

验证结果：

- 页面标题为 `叙事验证工具 基准时间轴原型 V1`。
- 数据集下拉存在 3 个选项。
- Canvas 和 Inspector 正常显示。
- 控制台 error 数量为 0。
- 点击画布事件块后，Inspector 切换到 `Selected Story Event`。
- 选中事件标题为 `电视台确定皮革厂专题`。
- `拆解事件` 按钮启用，`进入章节函数` 按钮保持禁用。

## 5. Git 边界

提交前需确认：

```powershell
git status --short --branch --ignored
```

预期边界：

- `验证作品/` 保持忽略，不提交。
- `app/*.log` 保持忽略，不提交。
- `.playwright-cli/` 保持忽略，不提交。
